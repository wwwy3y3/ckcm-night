extends layout

block script
    link(rel='stylesheet', href='/stylesheets/index.css')
    script
        $(document).ready(function(){
            $('.carousel').carousel({
              interval: 5000
            });

            
            $('.navbar').height($(window).height()*0.15);
            var remainHeight= $(window).height()-$('header').height();
            $('.carousel').find('.item').height(remainHeight);
          });

block content
    //h1.title Mr Dreamer
    div#myCarousel.carousel.slide
        div.carousel-inner
            div.active.item
                img(src="/images/lovestory.jpg")
                div.container
                    div.carousel-caption
                        h1 Mr Dreamer
                        p.lead Dare to dream. You are not the only one
                        //div.fb-like(data-send="false", data-width="450", data-show-faces="true")

            div.item
                img(src="/images/suspensedrama.jpg")
                div.container
                    div.carousel-caption
                        h1 Mr Dreamer
                        p.lead Dare to dream. You are not the only one
                        //div.fb-like(data-send="false", data-width="450", data-show-faces="true")


            div.item
                img(src="/images/poster.jpg")
                div.container
                    div.carousel-caption
                        h1 Mr Dreamer
                        p.lead Dare to dream. You are not the only one
                        //div.fb-like(data-send="false", data-width="450", data-show-faces="true")

        a.left.carousel-control(href="#myCarousel", data-slide="prev") &lsaquo;
        a.right.carousel-control(href="#myCarousel", data-slide="next") &rsaquo;
    //div.secondary
        div.container
            h2.sentence Dare to dream. You are not the only one
            button.btn.btn-inverse
                a(href="#") Facebook

  